# Ignore ALL files by default
*

# But explicitly KEEP these files
# Keep these files
!.gitignore
!README.md
!bed_management.exe
!bed_management     # for Linux/macOS

# Also keep source code if you want to backup
# (Remove the # below if you want to keep code in git)
# !models/
# !models/*.h
# !models/*.c
# !services/
# !services/*.h
# !services/*.c
# !utils/
# !utils/*.h
# !utils/*.c
# !main.c